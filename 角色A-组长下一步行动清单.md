# 角色A（组长/产品与集成）下一步行动清单

目标：让小组“不等人、不返工”，每周都有可演示闭环，最终顺利验收与答辩。

## 1. 你接下来 24 小时内要完成的 8 件事（按顺序做）

1) 定一期边界（写死）
- 一期必做：登录绑定、通知闭环、党团流程展示+个人进度、证明/申请审批闭环、基础导入导出、知识库基础查询
- 一期不做：校级系统接口对接、复杂AI生成、自动抓取公众号（可写“二期可选”）

2) 统一“可验收的闭环”
- 闭环1：管理端发布通知 → 学生端收到并已读统计
- 闭环2：配置入党/入团阶段 → 学生端看流程 → 管理端给某学生更新阶段 → 学生端看到变化
- 闭环3：学生提交证明申请 → 管理端两级审批 → 生成结果文件/状态 → 学生下载/查看结果

3) 建立 3 份“契约文档”（把协作锁死，减少扯皮）
- API 接口清单（见第4节模板）
- 数据字典（见第4节模板）
- 页面清单（见第4节模板）

4) 明确分工交付口（把“做什么”变成“交什么”）
- B（后端）：按 API 清单提供可调用接口 + 建表 SQL + 导入导出模板
- C（管理端）：按页面清单完成页面与接口对接，能演示发布通知/配置流程/审批
- D（学生端）：按页面清单完成小程序页面与接口对接，能演示收通知/看流程/提申请

5) 规定分支与合并纪律（不然最后一定集成爆炸）
- main 永远可运行；dev 做集成；feature-xxx 个人开发
- 合并到 dev 前必须：能跑、接口能通、写清变更点

6) 设定固定会议节奏（你只要照做就能推进）
- 每天 10 分钟：同步“昨天完成/今天计划/卡点”
- 每两天一次集成：合并到 dev，保证全链路可跑

7) 发一条“今天起执行”的组内公告（可直接复制）
我们按闭环优先做：先登录+通知闭环，再党团流程，再证明审批，最后导入导出和知识库。接口清单、数据字典、页面清单是协作契约，今天定版后按此开发。每两天必须合并一次到 dev，保证能跑能演示。

8) 把需求文档发给老师“确认范围/流程口径”
- 发 [需求文档-小组需求说明V1.md] 给老师
- 让老师只回答两件事：流程阶段是否一致、审批链路与提醒规则是否一致（先确认口径，别陷入细节）

## 2. 你接下来 3-7 天的工作重点

### 2.1 你亲自盯住的事情（不盯就会延期）
- “闭环演示”优先级永远最高：功能不全可以，但演示必须能跑通
- 契约文档不要频繁改：改一次全组返工一次，除非老师口径变化
- 集成日必须执行：宁可少写点新功能，也要保证系统整体可跑

### 2.2 你不用亲自写很多代码，但要把关这些点
- 权限与数据可见性：学生只能看自己的申请/进度
- 状态流转口径：申请单/审批记录/流程阶段的状态命名统一
- 导入导出可用性：模板表头固定、错误行可定位、导入结果可回显

## 3. 你每周要交付的“组长成果”

- 一份可演示版本（录屏也行）
- 一份更新后的：API 清单、数据字典、页面清单（只更新新增项）
- 一份周报（本周完成、下周计划、风险与需要老师确认的问题）

## 4. 你可以直接用的模板（复制填写即可）

### 4.1 API 接口清单模板
| 模块 | 接口名称 | 方法 | 路径 | 权限 | 请求参数（字段/类型/必填） | 返回字段 | 备注 |
|---|---|---|---|---|---|---|---|
| 登录 | 微信登录/绑定 | POST | /api/auth/login | 学生/管理员 | code:string(必填), studentId?:string | token, userInfo | 绑定失败提示 |
| 通知 | 发布通知 | POST | /api/notices | 管理员 | title, content, tags, audienceRule, attachments | id |  |
| 通知 | 通知列表 | GET | /api/notices | 学生/管理员 | page, size | list,total |  |
| 通知 | 标记已读 | POST | /api/notices/{id}/read | 学生 |  | ok |  |

### 4.2 数据字典模板
| 表名 | 字段 | 类型 | 是否必填 | 是否敏感 | 含义/口径 | 来源 | 备注 |
|---|---|---|---|---|---|---|---|
| student | student_id | varchar | 是 | 否 | 学号 | 导入 | 主键/唯一 |
| student | name | varchar | 是 | 否 | 姓名 | 导入 |  |
| student | phone | varchar | 否 | 是 | 联系方式 | 导入/维护 | 加密存储 |

### 4.3 页面清单模板
| 端 | 页面 | 路由/入口 | 主要功能 | 依赖接口 | 权限 | 验收点 |
|---|---|---|---|---|---|---|
| 管理端 | 通知管理 | /admin/notices | 新建/发布/列表/统计 | notices* | 管理员 | 能发布并看到发送记录 |
| 学生端 | 通知中心 | /pages/notices | 列表/详情/已读 | notices* | 学生 | 能收到并标记已读 |

### 4.4 集成日检查清单（每两天一次）
1) dev 分支可运行（后端能启动、管理端能打开、学生端能编译）
2) 闭环1（通知）跑通：发布→接收→已读
3) 接口返回格式一致（code/message/data）
4) 关键数据不泄露：学生看不到他人信息
5) 提交记录清晰：每个合并写明“新增页面/新增接口/影响范围”

### 4.5 会议纪要模板（10分钟站会也要留痕）
- 日期：
- 参会人：
- 昨日完成：
- 今日计划：
- 卡点/需要帮助：
- 需要老师确认的问题：
- 下一次集成日安排：

